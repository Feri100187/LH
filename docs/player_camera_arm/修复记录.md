# 越肩距离和待机袖臂

## 越肩镜头

后拉距离由 3.4 米缩短为 2.35 米，右侧偏移仍为 0.65 米。已同时写入 `LingshuiGame.ts` 默认值和两个游戏场景的脚本属性，避免 IDE 已加载组件使用旧默认值。实际 LayaAir 画面见 `third_person_close.png`。

## 袖臂问题

旧第一人称袖子从不规则裁切边直接挤出，产生长瘦三角折面和一条非流形边；它在待机时形成明显深槽和断节感。已对比游戏截图与 Blender 放大渲染，源模型可复现该形状，属于袖面拓扑问题。

修复前图片：`fps_idle_before.png`；源资产、导入前网格、脚本和场景备份位于 `backups/player_camera_arm_20260920/`。

现已重建为连续四边形袖面，并与独立袖口保持重叠贴合。仅更新 `FP.Sleeves weighted cut`（mesh index 13）；材质、骨序、绑定矩阵、两手握枪以及 Idle/Shoot 动作均与修改前一致。

## 检查结果

- 源模型 98 帧、GLB 回导 98 帧、Idle/Shoot 混合 75 个姿势检查通过。
- 袖面无开放边界、无非流形边；4:3 与 16:9 画幅均没有袖管末端入画。
- LayaAir 实际查看待机、呼吸另一阶段和连续射击恢复待机，凹槽与断节感已消除。截图：`fps_idle_after.png`、`fps_idle_breath_after.png`、`fps_return_idle_after.png`。
- 发布版实际确认近距右肩视角和新袖臂，控制台无警告或错误。两个场景结构校验与 TypeScript 检查通过。
- Web 与 single-html 已重新构建；single-html 本轮未单独运行验收。

详细几何检查在 `source_art/AnimeWatergunBoy/first_person_20260920/qa/sleeve_continuity.json`，导入映射复核在本目录 `mesh_mapping_check.json`。
