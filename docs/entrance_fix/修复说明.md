临湖建筑入口修复（2026-09-20）

补齐白色门廊两侧的实体回墙、连续顶板、门侧固定玻璃和中央固定玻璃；增加连续门头横框，统一门槛和门厅地面标高，并给门把手补上连接支座。入口不再从两侧、门头和门片之间透出草地。

同步拆分原先挡在门前约两米处的整楼碰撞包络，重新生成入口碰撞。人物现在可以走到真实门面前，门面及侧墙正常阻挡，门廊地面连续。

修改已写入：

- `assets/lingshui/LingshuiLake.glb` 的入口网格。
- `assets/lingshui/LingshuiEntrance_Fixed.lm`：独立入口网格，避免编辑器沿用旧子资源缓存。
- `assets/lingshui/LingshuiEnvironment.lh` 的入口网格引用。
- `assets/resources/LingshuiCollision.json` 的入口碰撞。
- `source_art/凌水湖_游戏修复.blend` 的可编辑源模型。

其他建筑、树木、材质与场景布局未改。原文件备份在 `backups/entrance_fix_20260920_090522/`。

验证包括：七项实际运行/通行检查全部通过；从正面、侧面和近景检查；GLB 与原生网格数据一致；门面十九组水平切片无透缝。预览图为实际构建版本，检查截图中临时隐藏了 HUD。
