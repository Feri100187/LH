# 颈长缩短

完整头部连同五官、耳朵、头发和眼镜一起下降 2.4 厘米，颈根固定，颈部上半段平滑压短。沿用加粗后的颈部宽度。

源工程：`source_art/AnimeWatergunBoy/neck_short_20260920/AnimeWatergunBoy_NeckShort.blend`，同目录 GLB 可继续导入。旧源工程保留。

游戏中已同步 14 个网格（13 个头部相关网格及颈部），而非只压缩颈部。导入清单与新旧哈希为 `native_mesh_update.json`，更新前原生资源备份位于 `backups/player_neck_short_20260920/native_meshes/`。

## 检查

- 全部 20 个源动作、564 帧检查：颈顶始终位于头部内部，最小重叠余量约 9.6 毫米；颈根 200 个顶点原位。
- 正面、侧面、背面、四分之三前后对比以及走、跑、跳、射击近景均已查看，无新增悬空或接缝。
- 骨架、绑定矩阵、权重、材质及动作保持兼容；其余 30 个网格数据逐字节一致。
- Web 与 single-html 已重新构建。源检查报告在模型目录的 `qa/`，single-html 本轮未独立运行验收。
- Web 发布版已实际查看第三人称待机及跳跃射击，44 个网格正常加载，头颈衔接连续，控制台无警告或错误。

重新导入完整最新源：

```text
python tools/prepare-player-assets.py --model-source source_art/AnimeWatergunBoy/neck_short_20260920/AnimeWatergunBoy_NeckShort.glb
```

等待 LayaAir 导入完成后，可使用 `tools/sync-player-geometry.py` 的 `--indices` 参数同步清单中的 14 个网格。
