# M1 连射时间失败尝试复核

本页只读复核 2026-09-21 的两份既有真实引擎记录，不修改其断言、原始数据或 FAIL 状态。输入来自测试工具合成触屏事件，不是硬件触屏证据。

## 记录与结论

- [corrected-continuous-60-fp.json](runs/20260921/corrected-continuous-60-fp.json)：整体 **FAIL**。移动射击窗口实际更新率为 **59.399 Hz**，输入持续 **2.0034 秒**。10 次发射对应 10 次接受的结算，其中 4 次靶命中。唯一失败断言为发射间隔：出现 **233.9 / 166.1 ms**，超过原固定容差 **200.000003 ± 24.666667 ms**。
- [corrected-continuous-240-fp.json](runs/20260921/corrected-continuous-240-fp.json)：整体 **FAIL**。静止/移动窗口实际 **217.315 / 218.171 Hz**，达到工具对 240 FPS 的原有 ±10% 门限；跳跃/切视角窗口仅 **215.687 / 210.551 Hz**，相应目标帧率仍为 **NOT TESTED**。静止、移动及切视角的间隔断言失败，原容差为 **200.000003 ± 12.166667 ms**。

两份记录中没有发射重复消费：60 FPS 四段依次为 10/10、10/10、10/10、10/10（发射/接受结算）；240 FPS 四段为 10/10、11/11、10/10、11/11。240 FPS 移动段输入持续 2.0076 秒，切视角段 2.1135 秒，不能按固定“恰好两秒”比较其数量。命中数还受移动、跳跃时准星与靶面交会影响，不能把总发射数当成命中数。

## 失败间隔附近的实际更新机会

单位均为同一页面 `performance.now()` 的毫秒。截止时刻按每段首发 `atMs + n × 200.00000298023224` 计算。表中前后更新为相邻的真实 `game.onUpdate` 入口采样，不是按目标 FPS 推算的帧。

| 记录/动作 | 发射 ID | 计划截止 | 前一次更新入口 | 下一更新入口 / 发射时间 | 两更新间隔 | 截止后延迟 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 60 / 移动 | 119 | 380934.900018 | 380932.900000（帧 21445） | 380983.400000（帧 21446） | 50.5 | 48.499982 |
| 240 / 静止 | 144 | 490154.200003 | 490130.400000（帧 27851） | 490195.500000（帧 27852） | 65.1 | 41.299997 |
| 240 / 静止 | 151 | 491554.200024 | 491529.400000（帧 28156） | 491593.800000（帧 28157） | 64.4 | 39.599976 |
| 240 / 移动 | 157 | 494348.900012 | 494329.500000（帧 28697） | 494391.400000（帧 28698） | 61.9 | 42.499988 |
| 240 / 切视角 | 177 | 501365.100009 | 501329.500000（帧 30111） | 501393.500000（帧 30112） | 64.0 | 28.399991 |
| 240 / 切视角 | 184 | 502765.100030 | 502729.700000（帧 30414） | 502795.900000（帧 30415） | 66.2 | 30.799970 |

这些迟发均发生在记录中**截止之后的首个实际更新帧**；中间没有另一条可供发射的游戏更新记录。60 FPS 的下一发 ID 120 在 381149.5 ms 发出，距上一发 166.1 ms，但对应原计划截止为 381134.900021 ms，仍晚约 14.6 ms。240 FPS 的 159.4、160.7、158.9、173.5 ms 短间隔同样是回到原单调时钟计划，未在同一帧补发多次。该解释不改变原有严格间隔断言的 FAIL。

对两份记录所有续发逐条检查：采用运行源码的 0.001 ms 比较余量，发射帧均与首条达到计划截止的更新入口帧一致。这里存在采样边界：工具先记录更新入口 `atMs`，然后调用原 `onUpdate`，运行时内部才读取发射时钟。本次有发射帧的两者差值为 **0–0.1 ms**；未发射帧没有单独保存内部时钟，因此不能由入口时间声称逐条证明内部时钟零误差。上表异常点距前一帧入口至少约 2 ms，并非贴近 0.1 ms 的边界案例。

## 因果与重测边界

可确认的是：平均更新率达标不代表不存在 50–66 ms 的单次更新空隙；上述间隔异常与实际更新空隙同现。记录没有 CPU 性能分析或事件循环任务栈，**不能证明**空隙由测试工具历史 JSON 序列化、游戏逻辑、垃圾回收、GUI 自动化或系统调度中的哪一项造成。

随后工具将活动期间的历史发布缩减为摘要，用新页面重测。此改动减少测量负担，不改变游戏射速、计时、断言或这些旧结果；新的通过或失败必须使用独立记录引用，不能覆盖本页的失败尝试。
